# Fixtures

Keep committed fixtures synthetic or pseudonymous. Personal configuration,
credentials, and conversation exports do not belong here. Tests generate their
configuration and process artifacts in temporary directories; the versioned Phase
5 benchmark is packaged under `soulmate_core/evaluation/data` so the installed CLI
can run it offline.
