{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main Soulmate window can use Tauri core window APIs only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
