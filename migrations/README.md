# Migrations placeholder

Alembic configuration and the first SQLite migration begin in Phase 1. No schema
or migration has been created. Every subsequent persistent schema change requires
a versioned migration and relevant upgrade/restart verification.
