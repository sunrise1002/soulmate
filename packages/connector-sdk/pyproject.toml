[project]
name = "soulmate-connector-sdk"
version = "0.1.0"
description = "Provider-neutral SDK for independently installable Soulmate connectors"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/soulmate_connector_sdk"]
