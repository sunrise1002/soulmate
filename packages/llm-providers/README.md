# Provider adapters placeholder

Reserved for Phase 3: deterministic fake, Ollama, and OpenAI-compatible providers.
Validate structured output and enforce egress policy. No provider SDK is installed.
