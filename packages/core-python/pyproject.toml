[project]
name = "soulmate-core"
version = "0.1.0"
description = "Infrastructure-independent Soulmate Personalization Kernel"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/soulmate_core"]
